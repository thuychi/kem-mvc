var DOM = require('../lib/dom.js');

module.exports = class Route extends DOM {
	render() {
		return {
			match: new RegExp('^' + this.props.path + '$'),
			controller: this.props.controller,
			action: this.props.action
		}
	}
}